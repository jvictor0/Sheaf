# Dresden 4 SDD Progress

Plan: `docs/superpowers/plans/2026-07-10-add-dresden-4-synth-app.md`
OpenSpec change: `add-dresden-4-synth-app`
Branch: `codex/dresden-4`

- Spec review: Claude Opus PASS after revisions for matrix modulation normalization, realtime/scope budget, and output-channel policy.
- Baseline: `make -C projects/synth test` exited 0 before implementation.
- Task 1: complete (commits bae1f72..6181c62, focused test `make -C projects/synth build/parameter_modulation_tests && projects/synth/build/parameter_modulation_tests`, Claude Sonnet review approved; OpenSpec 3.7-3.8 checked).
- Task 2: complete (commits 845aead..9e4d98f, focused test `make -C projects/synth build/dsp_tests && projects/synth/build/dsp_tests`, Claude Sonnet review approved after static decimator-shape fix; OpenSpec 3.1-3.6 checked).
- Task 3: complete (commits 5bb5aa3..7c0136a, focused test `make -C projects/synth build/module_tests && projects/synth/build/module_tests`, Claude Sonnet review PASS with no Critical/Important findings; OpenSpec 1.1-1.3 and 2.1-2.3 checked).
- Task 4: complete (commits fe5e544..752d947, focused tests `make -C projects/synth build/portable_ui_tests && projects/synth/build/portable_ui_tests`, `make -C projects/synth build/miniapp_system_tests && projects/synth/build/miniapp_system_tests`, and `projects/synth/apps/miniapp/build/encoder_component_geometry_tests`, Claude Sonnet review PASS after restoring `synth_miniapp::ScopePathMath` compatibility; OpenSpec 4.1-4.4 checked).
- Task 5: complete (commits 1bd2ca0..b90fe55, focused tests `make -C projects/synth build/dresden4_system_tests && projects/synth/build/dresden4_system_tests` and `make -C projects/synth build/module_tests build/dsp_tests && projects/synth/build/module_tests && projects/synth/build/dsp_tests`, Claude Sonnet re-review PASS after tightening the real matrix feedback delay, restoring generic matrix color, and adding missing system coverage; OpenSpec 5.2 and 5.4-5.8 checked, while split items 5.1/5.3 stay open for later UI/registration/controller-profile work).
- Task 6: complete (commit 51a7faa, focused tests `make -C projects/synth build/portable_ui_tests && projects/synth/build/portable_ui_tests` and `make -C projects/synth build/dresden4_system_tests && projects/synth/build/dresden4_system_tests`, Claude Sonnet review PASS with only low-severity notes about existing semantic-control bounds and static encoder action-delta conventions; OpenSpec 6.1-6.3 checked).
- Task 7: complete (commits 6179408..d5af591, focused tests `make -C projects/synth/apps/sheaf-patch test` and `make -C projects/synth/apps/miniapp test`, Claude Sonnet re-review PASS after hardening relaunch ownership order, strengthening the generic owner factory harness, and justifying the launcher harness runtime-source link; OpenSpec 7.1-7.4 checked).
- Task 8: complete (commit 5761b7c, focused tests `make -C projects/synth/apps/sheaf-patch test` and `make -C projects/synth sheaf-patch`, Claude Sonnet review PASS with informational notes only; OpenSpec 8.1-8.4 checked).
- Task 9: complete (commits c5fe7e6..b4dbb1c; focused red command `make -C projects/synth build/dresden4_deadline_tests` failed before Makefile wiring; green commands `make -C projects/synth build/dresden4_deadline_tests && projects/synth/build/dresden4_deadline_tests`, `make -C projects/synth build test`, `make -C projects/synth/apps/sheaf-patch test`, `make -C projects/synth sheaf-patch`, and strict OpenSpec validation exited 0; Claude Sonnet re-review PASS after adding runtime-path FIR response coverage; Claude Opus final branch review returned REVISE for the matrix-to-parameter path using two internal samples of delay; `b4dbb1c` moved quad modulator sampling before parameter evaluation, raised quad hidden-depth capacity to 8, and strengthened the consumer-path test; focused Claude Opus re-review PASS; live GUI evidence supplied by the user covered startup, Braid activation, bank navigation, scopes, and encoders, while automated launcher/audio/save-load/rate/deadline tests covered the remaining non-hardware smoke requirements; hardware-only controller feedback is a separate follow-up; OpenSpec verification complete).

# Synth Color Flow Coherence SDD Progress

Plan: `docs/superpowers/plans/2026-07-11-make-synth-color-flow-coherent.md`
OpenSpec change: `make-synth-color-flow-coherent`
Branch: `codex/dresden-4`

- Spec review: xagent Claude Opus PASS with no Critical/Important findings; four Minor tightenings incorporated before implementation.
- Task 1: complete (uncommitted task delta layered over existing staged Braid work by explicit safety choice; RED missing explicit color APIs, GREEN focused parameter/portable/browser plus JUCE and wider compile checks; native spec/quality re-review approved after non-finite input validation).
- Task 2: complete (uncommitted task delta; RED missing parameter-owned appearance and snapshot-only encoder APIs, GREEN parameter/portable/module/instrument/Braid plus randomized UI tests; native spec/quality re-review approved after JUCE geometry fixture migration, per-attempt snapshot candidate isolation, and cross-group capacity/stale-clear regression).
- Task 3: complete (uncommitted task delta; RED missing reusable indicator/scope/matrix role APIs, GREEN 39/39 module plus MiniApp/Braid/DSP/portable suites; native spec/quality review approved with no findings).
- Task 4: complete (uncommitted task delta; RED proved Braid snapshot override rewrote published indicator color, GREEN Braid/portable/module/launcher suites; native spec/quality review approved with no findings and literal independent palette coverage).
- Task 5: complete (uncommitted task delta; mutation RED proved MiniApp regression catches missing module indicator wiring, GREEN MiniApp/portable/module/JUCE draw suites; native spec/quality review approved with no findings; OpenSpec app-migration group complete).
- Task 6: complete (strict OpenSpec, full synth, JUCE/runtime, launcher, app-build, structural, and diff verification passed; xagent Claude Opus repeated the holistic library/Braid/MiniApp audit and returned PASS with no Critical/Important findings; all optional polish was resolved and the focused Opus follow-up also returned PASS; OpenSpec verification group complete).

# Ganged Random LFO SDD Progress

Plan: `docs/superpowers/plans/2026-07-15-ganged-random-lfo.md`
OpenSpec change: `add-ganged-random-lfo`
Implementation base: `74b7fe4a`
Workspace: managed linked worktree, detached HEAD

- Spec review: xagent Claude Opus returned REVISE; all Critical/Important contract tightenings were incorporated and strict OpenSpec validation passed.
- Baseline: focused DSP/portable/MiniApp/UI-boundary checks and `make -C projects/synth test` exited 0 before implementation.
- Plan review: native Codex plan corrected against actual paths/targets; xagent Claude Sonnet returned REVISE, valid findings were incorporated, one color-ownership inference was rejected against authoritative design text, and focused Claude re-review returned PASS.
- Task 1: complete (commit `8b5b1b3c`; required missing-header RED observed; full DSP binary GREEN independently reconfirmed; Claude Sonnet spec and quality verdicts PASS with no Critical/Important findings; OpenSpec 1.1–1.4 checked). Minor cleanup ledger: rename local `outputT` for clarity and add an explicit infinite-`muSeconds` rejection case before final review.
- Task 2: complete (commit `e1e6235b`; required missing-API RED observed; full DSP binary GREEN independently reconfirmed; Claude Sonnet spec and quality verdicts PASS with no Critical/Important findings; OpenSpec 2.1–2.5 checked). Minor cleanup ledger: cover the out-of-range `Output` accessor and describe the turnover test as structural/soak evidence rather than heap instrumentation.
- Task 3: complete (commit `4ab1c319`; required missing-snapshot-API RED observed; full DSP binary GREEN independently reconfirmed; Claude Opus spec and quality verdicts PASS with no Critical/Important findings; OpenSpec 3.1–3.2 checked). Minor cleanup ledger: decide whether to enforce the shipped-target `std::atomic<double>::is_always_lock_free` assumption after browser/JUCE builds are known.
- Task 4: complete (commit `0e44c997`; missing-header RED observed for portable/browser consumers and the inability to isolate the JUCE assertion RED was disclosed; portable geometry, browser command-buffer, JUCE MiniApp parity, and browser MiniApp GREEN were reported, with portable and browser command-buffer checks independently reconfirmed; Claude Sonnet spec and quality verdicts PASS with no Critical/Important correctness findings; OpenSpec 3.3–3.5 checked). Minor cleanup ledger: prevent the JUCE explicit-bounds fallback from expanding a present dot at pathologically tiny widget bounds.
- Task 5: complete (commit `31f3536a`; missing MiniApp gang/visualizer API RED observed; all 26 MiniApp system tests GREEN independently reconfirmed and the JUCE MiniApp suite passed in the implementation report; Claude Sonnet spec and quality verdicts PASS with no Critical/Important findings; OpenSpec 4.1–4.2 checked). Minor cleanup ledger: add direct includes for random-LFO types and document the full worst-case `12 + 12 * 4 = 60` parameter-capacity formula before final review.
- Task 6: complete (commit `c9058167`; missing third-panel builder/node RED observed across portable, system, JUCE, and browser consumers; portable and 27-case MiniApp system suites, browser command-buffer, UI boundary, and diff checks were independently reconfirmed, while JUCE and forced Emscripten builds passed in the implementation report; Claude Sonnet spec and quality verdicts PASS with no Critical/Important findings; OpenSpec 4.3–4.4 checked). Minor cleanup ledger: make the direct main-panel snapshot read fail closed explicitly and replace the duplicated literal voice count with the MiniApp snapshot alias.
- Task 7: complete (commit `41e376c4`; stale native/JUCE/browser dependency discovery was demonstrated before exact Make wiring; focused DSP/portable/MiniApp/browser/boundary checks, JUCE and browser builds, the full synth suite, strict OpenSpec, placeholder scan, diff check, and untouched legacy-tree check all passed and were independently repeated by Claude Sonnet; spec and quality verdicts PASS with no Critical/Important findings; OpenSpec 5.1–5.3 checked). Minor cleanup ledger: align the two new JUCE coverage citations with the established binary-name/colon style.
- Final cleanup: complete (commit `1629eb13`; resolved every accumulated per-task minor, added explicit lock-free target enforcement, tiny-widget and bounded-read fail-closed regressions, direct includes/capacity rationale, named snapshot alias, and coverage citation cleanup; DSP, portable, 28-case MiniApp system, JUCE, browser, command-buffer, boundary, and diff checks passed).
- Final whole-change review: xagent Claude Opus reviewed `74b7fe4a..1629eb13` and returned PASS for spec compliance, architecture/correctness, realtime/concurrency safety, test adequacy, and overall merge readiness, with no Critical/Important findings. Optional notes: the proposed missing native `DspMath.hpp` edge was already empirically disproven because its `DSP_HEADERS` -> library dependency rebuilds all three consumers; the relaxed-field seqlock note is theory-only on fully atomic shipped targets; stronger segmentation/order/fail-closed assertions are nonblocking polish.

# Scale Modulation Processing SDD Progress

Plan: `docs/superpowers/plans/2026-07-15-scale-modulation-processing.md`
OpenSpec change: `scale-modulation-processing`
Implementation base: `562be16ff6a04ebd0fb450387433f549b37e70fb`
Branch: `codex/scale-modulation-processing`

- Spec review: xagent Claude Opus returned PASS with no blocking issue; all Important plan/spec tightenings were incorporated, including full 64-bit randomized UI masks, distinct gesture labels through 64, browser rendered-command scope, accepted local UI cadence, across-voice route union, and explicit recycled-slot accounting.
- Baseline: `make -C projects/synth test` exited 0 before implementation. Braid4 deadline evidence: 48 kHz avg `1.25264 ms`, p99 `1.41017 ms`; 96 kHz avg `1.24633 ms`, p99 `1.38579 ms`.
- Execution policy: fresh native Codex implementer for each task; exact base-to-head package and xagent Claude two-verdict review after every task; Sonnet for Tasks 1, 2, 5, 6, Opus for Tasks 3, 4 and final whole-change review.
- Task 1: complete (commit `cc52c4c9`; required missing-observer RED observed; parameter modulation, Braid4 system, module, engine, and rig suites GREEN; xagent Claude Sonnet returned SPEC COMPLIANCE PASS and CODE QUALITY PASS with no Critical/Important findings; OpenSpec 1.1, 1.3, and 3.1-3.3 checked; 1.2 deliberately deferred to Task 3 where the full-scan oracle is implemented). Minor cleanup ledger: expose/use a direct top-level parameter count before the recycling phase instead of relying on a pre-materialization `ParameterCount()` snapshot in the Braid4 structural test.
- Task 2: complete (commits `3549f01b` and review fix `b7537817`; strict RED covered 64-bit boundaries, sparse visits, bit-63 UI/controller/persistence, and narrow selected-mask access; 218 parameter cases plus portable UI and instrument suites GREEN; xagent Claude Sonnet final SPEC COMPLIANCE PASS and CODE QUALITY PASS with no Critical/Important findings; OpenSpec 2.1-2.4 checked). Minor cleanup ledger: Task 5 must expand the randomized state machine through gesture 63 and remove its vestigial 32-bit loop cap; optionally replace the side-effect-only `SceneGestureIndex` calls with a named validator.
- Task 3: complete (commit `159a0dd1`; strict RED proved the active-route API absent; 222 parameter cases and 39 module cases GREEN; no production full-scan apply callers remain; xagent Claude Opus returned SPEC COMPLIANCE PASS and CODE QUALITY PASS with no Critical/Important findings; OpenSpec 1.2 and 4.1-4.5 checked). Minor cleanup ledger: restore alphabetical include order, centralize the repeated route-neutral tolerance, and consider a low-value constructor binding/identity-init helper only if final cleanup remains simple.
- Task 4: complete (commit `dda1aeee`; strict RED proved collection/accounting APIs absent; parameter modulation, engine, and MiniApp system suites GREEN; xagent Claude Opus returned SPEC COMPLIANCE PASS and CODE QUALITY PASS with no Critical/Important findings; OpenSpec 5.1-5.6 checked). Minor cleanup ledger: batch reset-all collection once rather than once per top-level control, document the standard bipolar 0.5 recycle predicate, and add a direct multi-level nested-view pin-balance test.
- Task 5: complete (commit `5d0bc6fb`; meaningful RED exposed the 2-vs-64 gesture topology mismatch; four prescribed integration binaries GREEN; deterministic parameter runs repeated with seeds `0x51A7`, `0xC0FFEE`, and `0xA11CE` over 250 steps; xagent Claude Sonnet returned SPEC COMPLIANCE PASS and CODE QUALITY PASS with no Critical/Important findings). Minor cleanup ledger: the deliberately symmetric source-to-slot and slot-to-source oracle loops are mildly redundant but valuable as belt-and-suspenders coverage.
- Task 6: complete (commit `baf2c608`; fixture/API RED plus breadth-first materialization assertion RED observed; focused Braid4 suites and `make synth-test` GREEN; strict OpenSpec and zero browser diff passed; xagent Claude Sonnet returned SPEC COMPLIANCE PASS and CODE QUALITY PASS with no Critical/Important findings; OpenSpec 6.1-6.4 checked). Recorded Task 6 timings: 48/192 kHz baseline avg/p99 `1.03810/1.06567 ms`, sparse `1.04574/1.07104 ms`; 96/384 kHz baseline `1.02816/1.04525 ms`, sparse `1.04270/1.11071 ms`. Minor cleanup ledger: rename/comment the currently-materialized dense-route upper bound so it is not mistaken for a full-capacity dense bound.
- Final cleanup: complete (commit `092a64d2`; direct top-level count, named gesture validation, shared neutral constants, once-per-group reset collection, bipolar recycle documentation, three-level pin/reuse coverage, and accurate dense-bound naming; compile/work-count REDs observed; focused and full synth suites plus strict OpenSpec GREEN). Deliberately retained the symmetric oracle checks and skipped the low-value constructor helper.
- Final whole-change review: xagent Claude Opus reviewed exact delta `2a5a51f..cfd5fc03` (run `xrun_20260716044912649_9f04e4d0`) and returned SPEC COMPLIANCE PASS and CODE QUALITY PASS with no Critical/Important findings, explicitly auditing audio equivalence, 64-bit UI/controller/persistence boundaries, route identity and settling, pointer lifetime/reuse, realtime safety, oracle independence, Braid4 equal-work/timing evidence, browser protocol stability, coverage, and every OpenSpec checkbox. Minor follow-up ledger: reserve recycled-slot storage to high-water if control-boundary allocation must be eliminated after async growth; add a multi-bit active-gesture visit-count assertion; retain awareness that timing ceilings are platform-sensitive despite generous bounds and deterministic counters.

# Absolute Encoder Mode SDD Progress

Plan: `docs/superpowers/plans/2026-07-16-add-absolute-encoder-mode.md`
OpenSpec change: `add-absolute-encoder-mode`
Workspace: managed linked worktree, detached HEAD

- Spec review: xagent Claude Opus PASS after correcting Controllers requirement wording, the post-arming proof, serialization task ordering, and raw-center-before-slew exactness.
- Baseline: `parameter_modulation_tests`, `viewmodel_tests`, `blocks_tests`, and `controllers_page_ui_tests` all passed before implementation.
- Task 1: complete (commits `8693e629` and review fix `653f8b1e`; compile/assertion REDs covered the renamed contract, authoritative-null parsing, and truthful absolute catalog index; four focused binaries GREEN; xagent Claude Sonnet re-review returned SPEC COMPLIANCE PASS and CODE QUALITY PASS with no remaining findings; OpenSpec 1.1–1.3 checked).
- Task 2: complete (commit `0ad708aa`; two genuine missing-API RED cycles; 252/252 parameter tests GREEN twice; xagent Claude Opus returned SPEC COMPLIANCE PASS and CODE QUALITY PASS after independently re-deriving the convex coefficients, active-set termination/exactness, KKT projection, and proof counterexample; OpenSpec 2.1–2.5 checked). Its cleanup ledger was resolved by Task 6 and the final realtime-safety fix: bipolar handler storage is described as normalized, the redundant assertion is gone, and the nontransactional throw path was replaced by rollback-backed mutation-free rejection.
- Task 3: complete (commit `a3de4ecc`; missing message/routing API RED; parameter, block, and view-model suites GREEN; xagent Claude Sonnet returned SPEC COMPLIANCE PASS and CODE QUALITY PASS after independently checking exhaustive switches, JSON/association round trips, owning-scene visible-cell routing, every modifier, and no-op boundaries; OpenSpec 3.1–3.2 checked). Minor design ledger: `ParamSetAbsolute` remains exposed in the generic system-message catalog with a retained fixed value, intentionally parallel to the existing fixed-delta `ParamIncDec` surface.
- Task 4: complete (commit `035ba434`; processor-boundary RED showed mapped absolute turns emitted nothing; parameter, rig, MiniApp system, and Braid4 system suites GREEN; xagent Claude Sonnet returned SPEC COMPLIANCE PASS and CODE QUALITY PASS with no findings after verifying exact 0/64/127 normalization, timestamp/address provenance, turnStep independence, raw-zero consumption, push/thru boundaries, and untouched relative decoding; OpenSpec 4.1–4.3 checked).
- Task 5: complete (commit `22c58e48`; RED isolated the missing relative-only step cue; view-model, Controllers page UI, portable UI, and parameter suites GREEN; xagent Claude Sonnet returned SPEC COMPLIANCE PASS and CODE QUALITY PASS with no findings after tracing the real portable action, preserved open presentation, structural non-deletability, persisted config, real profile factory, and restored relative step; OpenSpec 5.1–5.2 checked).
- Task 6: complete (commits `d9bc2f5c` and final-review fix `daa421d1`; 192 seeded randomized cases plus forced endpoint, aliasing, zero/one-weight, saturation, and post-arming proof topologies compare production storage against a spec-derived coefficient oracle and an independent bisection projection; parameter tests passed twice, UI-boundary and clean full synth reruns passed, and strict OpenSpec validation passed; OpenSpec 6.1–6.3 checked). Informational per-task review notes: the randomized range-kind toggle intentionally confirms normalized storage rather than distinct numeric bounds, and an armed zero-effective-weight latent value is outside the projection comparison while its active state is still checked.
- Final whole-change review: xagent Claude Opus initially returned SPEC COMPLIANCE PASS and CODE QUALITY FAIL because each absolute CC allocated three solver vectors on the audio thread and reachable post-arming failures could throw with partial mutation. RED coverage reproduced the malformed-latent throw and the missing fixed-workspace API. Commit `daa421d1` replaced the runtime path with the exact 130-location stack workspace, `noexcept` preflight/staging/rollback, and mutation-free invalid-state no-ops; 264 parameter cases, UI-boundary, and the complete synth suite passed afterward. Focused Opus re-review returned SPEC COMPLIANCE PASS, CODE QUALITY PASS, and APPROVE with no Critical or Important findings.
# Generic JUCE Portable UI Backend SDD Progress

Plan: `docs/superpowers/plans/2026-07-16-complete-generic-juce-ui-backend.md`
OpenSpec change: `complete-generic-juce-ui-backend`
Implementation base: `c72133f4fa2c5c0544d942a06e76ed7600edcc28`
Workspace: managed linked worktree, detached HEAD

- Proposal/plan: complete (commit `12ce3756`; persistent xagent Claude Opus proposal and plan reviews approved after all Critical/Important findings were incorporated).
- Baseline: `make -C projects/synth/apps/miniapp test` exited 0 before implementation.
- Execution policy: three coarse tasks; fresh native Codex implementer/fixer per task; one persistent xagent Claude reviewer reused for every task review and re-review; proposal reviewer session retained.
- Task 1: complete (commit `f4236bc0`; compile and behavioral REDs observed; full eight-executable miniapp JUCE suite GREEN; persistent xagent Claude Opus returned Spec Compliance PASS and Task Quality PASS with no Critical/Important findings; OpenSpec 1.1-1.2 checked). Minor ledger: native-peer focus assertions may be unavailable on headless CI; the flow equality primarily proves surface/local round-trip while other assertions carry flow correctness; the necessary FilePage surface-space assertion migration was outside the original two-file brief but explicitly reviewed and approved.
- Task 2: complete (commit `07df4c5c`; missing-viewport, shrink-clamp, and nonzero-scroll refresh REDs observed; full eight-executable miniapp JUCE suite GREEN; same persistent xagent Claude Opus returned Spec Compliance PASS and Task Quality PASS with no Critical/Important findings; OpenSpec 2.1-2.2 checked). Minor ledger: content extent floors use the live host size before first-pass bounds are assigned, and pure host resize does not recompute the visible-size floor until refresh; both only affect declared extents smaller than the viewport and should be considered in final cleanup.
- Task 3: complete (commit `8f4e34b3`; production runtime-shell regression, Controllers harness, and 250-step seeded simulation migrated to the shared `PortableComponent`; obsolete Controllers-specific renderer, host alias, tests, and build/boundary references removed; full synth, eight-executable JUCE, harness, 61-case browser, boundary, and strict OpenSpec matrix GREEN; same persistent xagent Claude Opus returned Spec Compliance PASS and Task Quality PASS with no Critical/Important findings; OpenSpec 3.1-3.2 checked).
- Final cleanup: complete (commit `8563d858`; corrected visible-floor scroll sizing on first refresh and pure resize with regressions, removed the obsolete flat-hosting helper, hardened the browser selector, and restored shell-test navigation hygiene; focused backend, full JUCE, harness, browser discovery, and diff checks GREEN).
- Final whole-change review: the same persistent xagent Claude Opus context reviewed exact delta `c72133f4fa2c5c0544d942a06e76ed7600edcc28..cd3ed96f` and returned `SPEC COMPLIANCE: PASS` and `CODE QUALITY: PASS` with no Critical/Important findings; every actionable accumulated minor was confirmed resolved.
- Final independent verification: `make -C projects/synth test`, `make -C projects/synth/apps/miniapp test`, `make -C projects/synth/apps/controllers_harness`, unsandboxed `make -C projects/synth/browser test` (61/61), `bash projects/synth/scripts/check_ui_boundary.sh`, strict OpenSpec validation, both forbidden-reference scans, and `git diff --check` all passed; the initial sandboxed Playwright failure was solely the expected macOS Mach-port denial and passed unchanged outside the sandbox.
- Post-review regression correction: a user smoke test exposed that hosted draw components were translating the existing MiniApp surface-space command buffers twice. RED rendered-image tests now cover nested node-local drawing, nested surface-space drawing, ambiguous line endpoints, complete draw-node coordinate classification, both real MiniApp scopes, and all sixteen real encoder nodes. Both generic backends now classify one coordinate space for each complete draw-node buffer and normalize it exactly once; there are no application IDs or page branches in either adapter.
- Correction verification: the focused JUCE and Playwright tests passed after meaningful REDs; the serial full synth suite, eight-executable MiniApp JUCE suite, Controllers harness build, unsandboxed browser suite (64/64), UI boundary check, strict OpenSpec validation, forbidden-reference scans, and `git diff --check` all passed. Manual captures of the rebuilt app confirmed the JUCE main page retains the original scope/encoder geometry and continuous waveform paths, while the Controllers page retains its corrected stacked rows and controls. The only broad-suite false start was the existing Braid deadline gate running concurrently with two other heavy matrices; the required serial rerun passed every deadline.
- Correction review: xagent Claude Opus run `xrun_20260717180820547_d4a06915` reconstructed the retained whole-change review history, audited the live correction, and returned `SPEC COMPLIANCE: PASS` and `CODE QUALITY: PASS` with no Critical/Important findings. Its two Minor edge findings—geometry-free shape commands voting against node-local buffers and JUCE classifying against rounded rather than portable float dimensions—received focused RED regressions, generic cross-backend fixes, GREEN full suites, and a same-session re-review that confirmed both resolved with no new findings and repeated both PASS verdicts.
# Runtime Button Grids SDD Progress

Plan: `docs/superpowers/plans/2026-07-16-add-runtime-button-grids.md`
OpenSpec change: `add-runtime-button-grids`
Implementation base: `d4498d81f3fc9cf1e8d82d6d636194671bb04cf0`
Workspace: managed linked worktree, detached HEAD

- Spec review: xagent Claude Opus returned PASS with no Critical/Important findings. Implementation cautions: explicitly zero `Color::a` for empty/disconnected cells; distinguish pressure terminology from the shared velocity byte; use established capacity/storage-address allocation evidence; duplicate registration is a topology contract.
- Baseline: `make -C projects/synth test` exited 0 before implementation.
- Execution policy: sequential native Codex implementers; xagent Claude task review after each exact base-to-head package; reuse the same implementer and reviewer for small fix/re-review loops; use fresh context for large fixes; final whole-change review on Claude Opus.
- Plan review: xagent Claude Sonnet returned NEEDS_CHANGES for incorrect/underspecified `Engine` destruction order and a source-breaking `MessageInBus` constructor insertion. The corrected plan specifies exact snapshot/facade/processor member order, leaves the sole positional-capacity constructor unchanged and attaches grids via `SetGridManager`, names all three output constructors, uses legacy `Flash<State>` naming, adds a profile schema constant, and honestly introduces the new pressure sidecar. The same Sonnet reviewer returned PASS with no remaining Critical/Important/Minor findings.
- Task 1: complete (commit `9fafb39b`; required missing-module and missing-StateCell REDs observed; focused button-grid/parameter tests and full synth suite GREEN; xagent Claude Sonnet returned spec-compliant and task-quality Approved with no Critical/Important findings; OpenSpec 1.1-1.5 checked). Reviewer minor about shorthand flash palette order was rejected after direct comparison with `/Users/joyo/theallelectricsmartgrid/private/src/SmartGrid.hpp`: inverse flash colors are the exact legacy port behavior, not an accidental swap.
- Task 2: complete (commit `e611221c`; required missing-message and dual-manager routing REDs observed; focused button-grid/parameter/instrument/blocks/view-model tests and full synth suite GREEN; xagent Claude Sonnet independently rebuilt the required binaries and returned spec-compliant and task-quality Approved with no Critical/Important findings; OpenSpec 2.1-2.4 checked). Minor ledger: grid messages intentionally remain absent from the user-facing generic system-message-kind dropdown until Task 6 supplies dedicated Grid Button/Grid Block concepts; the pressure factory's reuse of the identical press payload constructor is harmless implementation style.
- Task 3: complete (commit `018da635`; required missing-facade/ownership RED and immediate-initial-publication RED observed; focused Engine/Instrument/Rig/MiniApp, output-protocol, and full synth suites GREEN; xagent Claude Opus independently reproduced the focused and output regression results and returned PASS with no Critical/Important findings; OpenSpec 3.1-3.3 and 5.1-5.3 checked). Minor ledger: `GestureColor()` relies on its caller's established parameter-state null guard; the grid-slot null check is harmless defensive code for currently always-allocated snapshots; initial grid publication versus deferred parameter publication is intentional compatibility-preserving asymmetry.
- Task 4: complete (commit `a193309e`; required missing-poly-pressure API RED observed; focused Instrument/Blocks/Rig, parameter-output regression, and full synth suites GREEN after advancing one stale invalid-future-schema fixture from 2 to 3; xagent Claude Sonnet independently reran focused and full verification and returned PASS with no Critical/Important findings; OpenSpec 4.1-4.5 checked). Minor ledger: absent optional `pressureInput` is intentionally omitted rather than serialized as null.
- Task 5: complete (commits `4f7b613e` and boundary correction `2ea3a279`; separate expansion/reconstruction REDs plus duplicate-append audit RED observed; Blocks/ViewModel and full synth suites GREEN; initial xagent Claude Sonnet review found an Important task-boundary violation caused by the root dispatch over-scoping view-model work into Task 5; the same implementer restored all three view-model files byte-for-byte to base, and the same reviewer independently verified the corrected exact three-file delta and returned PASS with no Critical/Important findings; OpenSpec 6.1-6.3 checked). Minor ledger: the public append API's duplicate-against-existing-accumulator check is not exercised by current production callers but intentionally preserves all-or-nothing behavior for non-empty caller accumulators.
- Task 6: complete (commit `5b9653a1`; separate ViewModel, portable, generic-JUCE, malformed-number simulation, and hidden-orphan candidate-placement REDs observed; focused headless and MiniApp/JUCE suites GREEN with deterministic grid simulation seed `0x6a1d2026`, 320 operations/200 accepted; full functional suite and UI boundary GREEN, while the unrelated 96 kHz deadline p99 benchmark was host-scheduling-sensitive under full-suite load and passed immediately in isolation; xagent Claude Opus independently reran headless/JUCE/deadline verification and returned PASS with no Critical/Important findings; OpenSpec 6.4-6.6 checked). Minor ledger: failed edits preserve committed state atomically but leave the attempted value in the open in-session row, matching pre-existing block-editor behavior.
- Task 7: complete (commit `f9da0bc7` plus traceability review fix; focused core/message/MIDI/persistence/engine/Controllers suites, full synth suite, warning-enabled app build, UI boundary, generic JUCE backend/runtime-shell coverage and seeded simulation, strict OpenSpec validation/status, placeholder/diff checks, finalized-grid storage/pointer stability, unchanged MIDI output goldens, and all 52 normative scenario mappings passed; final reviewer requested only that verified OpenSpec 7.1-7.4 and this progress entry be recorded, which the follow-up completed; application-level grid exposure remains an intentional non-goal).

# Browser App Catalog Launcher SDD Progress

Plan: `docs/superpowers/plans/2026-07-18-browser-app-catalog-launcher.md`
OpenSpec change: `add-browser-app-catalog-launcher`
Branch: `codex/browser-app-catalog-launcher`
Implementation base: `d0d14472`

- Plan review: xagent Claude Opus run `xrun_20260719053557460_5b0c3142` approved after correcting active server/config paths, native contract-test scope, package MIME headers, activation-task phasing, loader threading, and command working directories.
- Baseline: `make test` in `projects/synth` and the browser TypeScript/Node/Playwright suite (64/64 after building fake-app and miniapp WASM fixtures) exited 0 before implementation.
- Task 1: complete (commit `72d4c928`; 20/20 Node catalog/scaffold tests, 6/6 runtime-core Playwright tests, generic-runtime check, and native `browser-unit-test` GREEN; persistent xagent Claude Sonnet run `xrun_20260719060400106_e0067e21` returned spec compliant and Task quality Approved; OpenSpec 1.1–1.4 checked). Controller resolved cross-app package-path reuse as allowed within trusted publisher catalogs and catalog-version-to-worker threading as deferred to Task 4 after Task 3 materialization. Minor ledger for final review: identifiers have an implementation cap of 200 characters beyond the normative character-pattern constraint.
- Task 2: complete (commits `57ea7893` and review fix `aaae9ce3`; TypeScript/generic-runtime checks, 4/4 Node catalog tests, 16/16 focused launcher/runtime/static Playwright tests, 3/3 fake-app and 6/6 miniapp real-WASM tests GREEN; persistent xagent Claude Sonnet run `xrun_20260719062757768_078b5965` initially reproduced a Critical delayed-launcher ownership race plus retry and selection-shell defects, then independently verified the DOM-identity ownership fix, preserved healthy retry state, exact pending-shell check, all regressions, and returned spec compliant and Task quality Approved; OpenSpec 2.1–2.4 checked). Minor compatibility-label finding resolved with an explicit invariant comment.
- Task 3: complete (commits `d7de26d1` and review fix `a0a07670`; deterministic assembler 5/5, package-loader/two-origin 11/11, Node 31/31, audio/MIDI/persistence 15/15, runtime/static/fake-app 13/13, TypeScript build, and generic-runtime boundary GREEN; persistent xagent Claude Opus run `xrun_20260719071911546_1cf3a75c` returned spec compliant, initially found an Important compressed-transfer `Content-Length` interoperability defect and a Minor divergent path grammar, then verified authoritative decoded-byte SHA-256 handling, bad-digest pre-import rejection, the shared strict normalizer, and returned Task quality Approved; OpenSpec 3.1–3.5 checked). Task 6/9 integration ledger: exercise the real remotely materialized Emscripten package with pthread/Wasm-worker/AudioWorklet sidecars end-to-end, including rewritten `entryUrl` with `mainScriptUrlOrBlob`; Task 3 proves the mappings and a remote generic package but not that full real-sidecar combination.
- Task 4: complete (Phase A `17ef3271`, Phase B `ac681fdc`; activation 8/8, audio 7/7, MIDI 7/7, fake-app 3/3, miniapp 6/6, launcher/runtime 13/13, TypeScript build, generic-runtime boundary, and diff check GREEN; persistent xagent Claude Opus run `xrun_20260719080741275_cd84e83a` traced the real synchronous click stack and ownership graph, returned spec compliant and Task quality Approved with no Critical/Important findings; OpenSpec 4.1–4.5 checked). Controller-approved implementation variance recorded in the task wording/report: selected apps use the host AudioBridge/shared ring on the exact leased JavaScript AudioContext and never invoke the native Wasm AudioWorklet callback that would create a second opaque context; the focused direct native API remains. Launcher MIDI denial is deliberately fatal/retryable per `sbac-6`, while the focused legacy direct API remains tolerant. Minor final-review ledger: nested failure catches may call package/lease disposal redundantly, relying on their required idempotence; keep or comment as defense-in-depth if cleanup is touched later.
- Task 5: complete (commit `aa59cb1a`; native browser contract, canonical Node 31/31, persistence 5/5, affected browser 29/29, full Playwright 96/96, real-WASM restart persistence, and generic-runtime boundary GREEN; persistent xagent Claude Opus run `xrun_20260719083241614_72d1da16` audited real JS/native wiring, validation parity, ABI string lifetime, mount/populate/init ordering, build-ID exclusion, publisher isolation, and returned spec compliant and Task quality Approved with no Critical/Important findings; OpenSpec 5.1–5.3 checked). Minor Task 9 cleanup ledger: add Playwright `testMatch: "**/*.spec.ts"` so browser discovery no longer imports Node `*.test.mjs` files in parallel; optionally add a direct native `Initialize(pub, app, 2) == -1` assertion even though the throwing derivation and catch-before-start path are already covered. Shared `/data/config.json` and `/data/logs` across publishers are intentional compatibility behavior, not a leak.
- Task 6: complete (commit `9bccc25b`; deterministic/transactional first-party publication, launcher-only root, canonical same-deployment catalog and nested immutable package, complete rollback mapping, Cloudflare headers, 36/36 Node tests, 99/99 full Playwright before the final rollback regression, static-site 6/6, fake 3/3 and real miniapp 7/7 smoke, generic-runtime boundary, strict OpenSpec, and artifact/digest inspection GREEN; persistent xagent Claude Opus run `xrun_20260719091222135_931d557d` returned spec compliant and Task quality Approved with no Critical/Important findings; OpenSpec 6.1–6.5 checked). The real two-origin catalog-selected miniapp test proves blob-rewritten entry/main-script, pthread worker, WASM, and AudioWorklet behavior and closes the Task 3 integration ledger. Minor final-review ledger: make the required fake-app-then-miniapp build ordering less implicit, and detect unexpected future Emscripten emission sidecars rather than silently limiting the publication inventory to today's JS/WASM pair. Live Cloudflare `_headers` behavior remains a Task 7 deployed-origin check.
- Task 7: complete (commits `8c69a648` and review fix `9b9b25d8`; official full-SHA GitHub Pages workflow, deterministic publisher-only artifact, deployed CORS/MIME/size/hash/build-ID validator, production-like remote Chromium smoke, 53/53 root-CWD Node workflow gate, focused browser 19/19, full Playwright 101 pass plus one intentional remote-unset skip, strict OpenSpec, and diff checks GREEN; persistent xagent Claude Opus run `xrun_20260719095149466_95f60862` initially reproduced a Critical CI-root working-directory failure, then independently verified the module-relative path fix and subprocess regression plus the official Pages-limits documentation, returning spec compliant and Task quality Approved; OpenSpec 7.1–7.5 checked). All six action SHAs were verified directly against their official upstream repositories. Live Pages CORS/MIME and the deploy output URL remain deliberately fail-safe CI-owned evidence; no deployment or external repository settings were changed.
- Task 8: complete (commit `c2f2e142`; exact schema-v1 and multi-app publisher guide, browser README links, `sbac-1`–`sbac-12` plus modified `sprs-12` coverage mappings, 53 Node and 101 Playwright tests with one configured remote skip, both publication commands, Markdown/source links, strict OpenSpec, obsolete-guidance scan, and diff check GREEN; persistent xagent Claude Sonnet run `xrun_20260719101738515_0e563237` checked every normative field/path/command against source, all 31 quoted test titles, both JSON examples, link resolution, evidence boundaries, and returned spec compliant and Task quality Approved with no findings; OpenSpec 8.1–8.2 checked). The first sandboxed Chromium attempt failed before test execution on the known macOS Mach-port denial; the unchanged suite passed with the required unsandboxed permission.
- Task 9: complete (commit `19280ecc`; serial native suite across 24 binaries, 56/56 Node from both workflow-root and package entry points, ordered catalog/package/fake/real/audio/MIDI/persistence/UI/publication browser gates, full serial Chromium 101 pass plus one live-only skip, local Pages equivalent 2/2, both deterministic publications, strict OpenSpec, diff, artifact/digest, and protected-path inspections GREEN; persistent xagent Claude Opus run `xrun_20260719103948256_809ef3f2` verified the cleanup ledger and surrounding source, returned spec compliant and Task quality Approved with no Critical/Important findings; OpenSpec 8.3–8.5 checked). Focused hardening restricted Playwright discovery to browser specs, added direct native runtime-config rejection coverage, removed fake/miniapp rollback-byte writer coupling, and rejected unexpected future `miniapp.*` emissions before atomic replacement. Final production evidence: startup fetched only source/catalog bytes, selection fetched the two immutable verified miniapp files and exercised real blob entry/main-script, pthread/WASM/AudioWorklet/audio startup, `app.js` exists only under rollback, and the Pages artifact contains only the catalog/package tree. Live deployed-origin CORS/MIME remains CI-owned and unproven locally.
- Final whole-change review: fresh xagent Claude Opus run `xrun_20260719104518669_96f04007` audited `d0d14472..f28e3d52`, returned spec compliant and merge readiness Approved with no Critical/Important findings, and identified two optional Minors. Commit `2c220d9b` removed the unreachable launcher-owned return control, documented page-level reload/navigation, and explained the verified-vs-rewritten entry-blob invariant; the same Opus context re-reviewed `d0d14472..2c220d9b`, confirmed both Minors resolved with no findings outstanding, and repeated merge readiness Approved. Fresh controller verification at final code head passed the full native suite, required fake/miniapp build order, 56/56 Node tests, 101 local Chromium tests plus one intentional live-only skip, both deterministic publication commands, strict OpenSpec validation, and diff/scope hygiene. Live GitHub Pages and Cloudflare response headers remain external CI/deployment evidence and were not claimed or mutated.
# Native Browser Audio and Generic App Packaging SDD Progress

Plan: `docs/superpowers/plans/2026-07-19-native-browser-audio-and-generic-app-packaging.md`
OpenSpec change: `fix-browser-native-audio-and-generic-app-packaging`
Branch: `codex/browser-app-catalog-launcher`
Implementation base: `a14cabf9b2ecfd27f0b0a918a993af2cf6c34979`

- Plan review: persistent xagent Claude Opus run `xrun_20260719165552040_f47db302` returned ready after a same-session re-review confirmed the pinned-Emscripten probe gate, post-start memory-growth assertion, sibling-change isolation, and preserved Mini App category resolved every finding.
- Execution policy: six sequential native Codex implementers; persistent xagent Claude reviewer per task for spec/quality/fix re-review; fresh Claude Opus whole-change release review; protected untracked `projects/synth/browser/package-lock.json` and `projects/synth/miniapp/` remain untouched.
- Task 1: complete (commit `a6f7bb28`; exact Emscripten 6.0.3 adopted-context probe advanced 8 native blocks on the supplied handle; native contract, TypeScript/generic scan, focused Playwright 23/23, Node package checks 14/14, and real Mini App Wasm smoke 7/7 GREEN; persistent xagent Claude Opus run `xrun_20260719173022761_b94f6ef5` returned SPEC COMPLIANCE PASS and CODE QUALITY PASS; OpenSpec 1.1–1.4 checked). Minor ledger for Task 6: optionally document the worker client's intentional no-audio boundary, consider hard-unload worker teardown semantics, avoid describing the uint32 finite check as deadline health, and add zero-context-registration/start-after-shutdown branch coverage. The native lifetime uncertainty is resolved by the existing page-lifetime retention of the stopped erased runtime/worklet stack when no synchronous join is available.
- Task 2: complete (commit `68ebefeb`; parser/builder/scaffold 19/19, manifest-derived generic boundary, fake fixture real compile, and real Braid 4/Mini App Emscripten build GREEN; persistent xagent Claude Opus run `xrun_20260719175843168_c2a40391` returned SPEC COMPLIANCE PASS and CODE QUALITY PASS; OpenSpec 2.1–2.4 checked). Task 3 landing gate: legacy publication is intentionally broken between Tasks 2 and 3 and must be migrated before deployment/merge. Minor ledger: isolate fixture output/report or otherwise prevent parallel Make races and stale fake directories, confirm the intended Braid 4 category casing, and retain the intentionally stricter case-sensitive substring boundary scan semantics.
- Task 3: complete (commit `c8d8bb1c`; neutral multi-app REDs plus fixture isolation, whole-generation atomicity, role-sensitive build IDs, and cross-app-alias hardening; required Node 30/30, full Node 72/72, real Braid 4/Mini App build, site/pages publication, generic boundary, and unchanged Braid source GREEN; persistent xagent Claude Opus run `xrun_20260719183237589_ab3f66a2` returned PASS with no actionable correctness, security, atomicity, race/TOCTOU, schema-validation, or test-gap findings; OpenSpec 3.1–3.4 and 4.1–4.2 checked). Non-blocking ledger: duplicated catalog-version computation, flat-emission assumption in the header glob, and standalone catalog/source-map replacement are acceptable because enforced emissions are flat and production swaps the fully staged site.
- Task 4: complete (commits `e3cfc420` and report `f060b2e3`; real Braid 4/Mini App table acceptance, whole-catalog deployed validation, generic CI/build targets, and host-artifact parity; Node workflow/publication 24/24, focused Playwright 22 pass plus two documented remote-unset skips, opt-in deployed-origin 2/2, two-app/four-file validator, generic boundary, and diff check GREEN; persistent xagent Claude Opus run `xrun_20260719190323122_31275be7` APPROVED with no actionable findings; OpenSpec 4.3 and 5.1–5.3 checked). Low observations: `--no-package-lock` preserves the protected lockfile choice at a dependency-reproducibility tradeoff, rollback intentionally reuses immutable catalog packages, and ABI-v2 static/workflow companion fixtures were justified outside the initial file list.
- Task 5: complete (commit `aa1725c1`; native-only runtime/publisher docs, one-record onboarding, uniform memory policy, two-app publication, whole-catalog validation, and `sbap-1`–`sbap-4`/`sprs-8` coverage mapping; documentation build/workflow suite 16/16 and obsolete-guidance searches GREEN; persistent xagent Claude Sonnet run `xrun_20260719191543782_e79cd21b` initially found three stale citations, then same-session re-review PASS with no findings; OpenSpec 6.1 checked).
- Task 6: complete (commits `44b38a7e` and `b9f954d1`; full browser prerequisite/ABI-v2 fixture hardening and final stale cross-origin ABI correction under strict RED/GREEN; final native contract, browser build/generic scan, Node 74/74, real two-app build, full Playwright 99 pass plus exactly two live-only skips, complete 24-binary synth/UI-boundary suite, Braid deadline 5/5, both publications, exact four-file package hash/size/MIME inspection, strict OpenSpec, diff and protected-path checks GREEN; persistent Task 6 Opus `xrun_20260719195910097_d16f789a` returned SPEC PASS/QUALITY PASS with no Critical/Important findings; fresh whole-branch Opus `xrun_20260719200617434_1684d081` found one stale ABI-v1 fixture Minor, then same-context final-HEAD re-review returned SPEC PASS, QUALITY PASS, unresolved actionable findings 0, MERGE APPROVE; OpenSpec 6.2–6.4 checked). Exactly two deployed-origin tests remain skipped without a configured remote URL; expected Emscripten pthread/growth and deprecated `USE_PTHREADS` warnings remain; live host headers are CI/deployment-owned. No push, deployment, Pages enablement, archive, or external mutation occurred.

# Relative Bank MessageIns SDD Progress

Plan: `docs/superpowers/plans/2026-07-23-add-bank-navigation-messageins.md`
OpenSpec change: `add-bank-navigation-messageins`
Branch: detached managed Codex worktree
Implementation base: `9060b347`

- Execution policy: two sequential native Codex Terra implementers; fresh xagent Claude Sonnet spec/code reviewer per task with same-session fix re-review; fresh xagent Claude Opus whole-change review.
- Baseline: focused `parameter_modulation_tests`, `blocks_tests`, and `viewmodel_tests` targets compiled successfully before implementation.
- Task 1: complete (commit `2f1babd5`; focused build and `parameter_modulation_tests` GREEN; xagent Claude Sonnet run `xrun_20260724054240389_b63faa8a` returned spec compliant and Task quality Approved with no Critical/Important findings; OpenSpec 1.1-1.3, 2.1-2.3, and 4.1 checked). Exhaustive-switch warnings in MIDI config layers are intentionally owned by Task 2.
- Task 2: complete (commits `e8a5b0e1` and review fix `df225c3c`; focused build plus `parameter_modulation_tests`, `blocks_tests`, and `viewmodel_tests` GREEN; xagent Claude Sonnet run `xrun_20260724055638083_f83e37d1` returned spec compliant and Task quality Approved, then same-context re-review confirmed its lone stale-comment Minor resolved with no findings remaining; OpenSpec 1.4-1.5, 3.1-3.4, and 4.2 checked). The full suite's only reported failures were two unrelated 96 kHz Braid 4 deadline-performance thresholds; controller verification will rerun the suite.
- Controller verification: fresh focused rebuild plus all three focused binaries GREEN; complete `make -C projects/synth test` GREEN, including all 44.1/48/96 kHz Braid 4 deadline cases; exhaustive relative/absolute bank switch audit complete; strict OpenSpec validation passed; implementation diff check clean; OpenSpec 5.1-5.3 checked.
- Final whole-change review: fresh xagent Claude Opus run `xrun_20260724060705924_a7f32b1d` returned spec PASS and merge APPROVE with no Critical/Important findings. Commit `5a897f63` corrected the enum-index comment, made RandomMod precedence uniquely observable, and added invalid-slot relative navigation to the randomized oracle; the same Opus context re-reviewed the complete package, confirmed all three Minors resolved with no new findings, and repeated APPROVE. Fresh controller verification at final head passed the three focused binaries, complete synth suite, strict OpenSpec validation at 17/17 tasks, and diff hygiene.

# Synth Master Clock and MIDI Sync SDD Progress

Plan: `docs/superpowers/plans/2026-07-19-synth-master-clock-midi-sync.md`
OpenSpec change: `add-synth-master-clock-midi-sync`
Implementation base: `5426bb41fb5228cbc725f6356c59aa86cbe1aeb1`
Workspace: managed linked worktree, detached HEAD

- Spec review: persistent xagent Claude Opus returned PASS after the numeric timing tolerances, output sample/time epoch mapper, cross-bus ordering, ADSR publication, `Phasor2Tick` authority, and exact `AudioBlock` plan identity were tightened; strict OpenSpec validation passed.
- Plan review: persistent Opus returned REVISE after finding a mistranscribed PLL phase policy and an implicit scheduled-event seam. The plan now uses the normative quarter-error/quarter-pulse/two-pulse policy, defines the Task 2 producer interface used by Task 3's test sink and Task 4's concrete lane, and requires three-part Task 3 evidence; same-session re-review returned PASS with no remaining Critical or Important findings.
- Execution policy: eight coarse sequential tasks; native Codex Sol implementers throughout, with highly prescriptive briefs that resolve architecture, invariants, exact seams, and required evidence before dispatch; same implementer handles small review fixes; one persistent Opus reviewer handles every task review/re-review and the final whole-change review.
- Persistent reviewer: xagent run `xrun_20260719190651915_18a4113c` (kept open through completion).
- Baseline: browser WASM build plus unsandboxed `npm test` passed all 64 Playwright cases and 6 Node cases. Core functional binaries passed; the existing Braid wall-clock deadline gate failed intermittently inside the full suite while other agent/browser processes were active, then the unchanged isolated deadline binary passed all five rate/sparsity cases. Treat that benchmark as load-sensitive and require isolated timing verification.
- Task 1 (clock primitives, affine plans, mapper): complete (implementation/report commits `439a8d26`/`d6eb5269`; review-fix/report commits `94c208d7`/`6691cab6`; missing-header REDs, forced DSP/master-clock/contract/engine GREEN with 17 clock cases, ASan/UBSan GREEN; persistent Opus initial and same-context re-review both returned SPEC COMPLIANCE PASS and CODE QUALITY PASS with no remaining findings; OpenSpec 1.1–1.3 and 2.1–2.2 checked; mapper-only portion of 7.5 verified but the composite item remains open for sender integration).
- Task 2 (transport, PLL, arbitration, crossings): complete (implementation/report commits `3d110a33`/`394d42b9`; review-fix/report commits `758f0053`/`5efde265`; missing-contract RED, 37/37 focused GREEN, warning-free affected rebuild, ASan/UBSan GREEN, and full synth suite GREEN including all Braid deadline cases; persistent Opus initial and same-context re-review both returned SPEC COMPLIANCE PASS and CODE QUALITY PASS with no remaining findings; OpenSpec 2.3–2.6 and 3.1–3.4 checked; clock-side 7.5 producer/crossing portion verified while the composite item remains open for Task 4 sender integration).
- Task 3 (MIDI ingress, Engine, SynthRig, config v2): complete (implementation/report commits `38ba6ef8`/`bc60dd02`; review-fix/report commits `d256256f`/`f4724136`; three intentional missing-contract RED groups; focused and full suite GREEN, ASan/UBSan GREEN, all Braid deadlines GREEN, strict OpenSpec valid; persistent Opus initial and same-context re-review both returned SPEC COMPLIANCE PASS and CODE QUALITY PASS with no remaining findings; OpenSpec 4.1–4.4, 5.1–5.4, and 6.1–6.3 checked).
- Task 4 (scheduled sender and JUCE): complete (implementation/report commits `340cf082`/`855a8d90`; review-fix/report commits `a5651208`/`c5560d3f`; core and JUCE missing-contract REDs; 20/20 sender GREEN plus repeated race runs, ASan/UBSan and TSan GREEN, full core/JUCE runtime/app build GREEN, strict OpenSpec valid; persistent Opus initial review found and same-context re-review cleared one lost-wakeup Important and one provider Minor, final SPEC COMPLIANCE PASS and CODE QUALITY PASS with no findings; OpenSpec 7.1–7.5 checked).
- Task 5 (browser timestamped MIDI): complete (implementation/report commits `3b02f32f`/`eb74f2b4`; strict RED/GREEN covered the C++ bridge/runtime ABI, Node timing, real fake-app and MiniApp Wasm, and the full 67-case browser suite; root independently reran the canonical `make -C projects/synth/browser test` fixture on ports 4173/4174 with 12/12 Node and 67/67 Playwright tests GREEN; persistent Opus returned SPEC COMPLIANCE PASS and CODE QUALITY PASS with no code findings after re-deriving additive 25 ms scheduling, cutoff margin, mixed-sink wakes, bounded priority, epoch conversion, ABI, reconnect clearing, diagnostics, and RT concurrency; OpenSpec 8.1–8.4 checked).
- Task 6 (portable Sync page): complete (implementation/report commits `f04bc84f`/`b49d3694`; three strict RED/GREEN groups covered JUCE-free staging/navigation, packed next-block Engine handoff plus coherent diagnostics stress, and JUCE/browser host parity; focused core/JUCE/real-Wasm, 12/12 Node, 68/68 Playwright, full synth including every Braid deadline, strict OpenSpec, scope, and diff verification GREEN; persistent Opus returned SPEC COMPLIANCE PASS and CODE QUALITY PASS with no code findings after independently proving the seq_cst publication ordering, config startup/save semantics, page lifecycle, host parity, visual bounds, and base-only JUCE aggregate defect; OpenSpec 9.1–9.4 checked). Informational ledger for Task 8: the pre-existing MiniApp JUCE aggregate cannot compile because unchanged `ControllersPageSimulationTests.cpp` references nonexistent `synth_runtime::ControllersTreeRenderer`; relevant Task 6 JUCE binaries pass independently, and both root and Opus verified the defect at base `a2a8e891`.
- Task 7 (MiniApp ADSR and tempo): complete (implementation/report commits `091aa9b1`/`07e4036b`; strict topology, clock/authority, and Braid fractional-query RED/GREEN groups; root independently reran MiniApp 37/37 and Braid 25/25; Task 7 deadline passed two unchanged reruns while an exact Task 6 base worktree failed two runs in different 44.1/96 kHz cases, proving the observed benchmark spikes are host scheduling noise; persistent Opus returned SPEC COMPLIANCE PASS and CODE QUALITY PASS with no actionable findings after auditing 17/272 storage, source-7 pointer stability, half-quarter gate boundaries/Stop-low, same-frame two-voice publication, rejected tempo caching/manual restoration, every-subframe Braid queries, continuity, RT safety, and the base-only JUCE fixture defects; OpenSpec 10.1–10.5 checked). Task 8 ledger: repair or explicitly disposition both pre-existing MiniApp JUCE aggregate defects and preserve isolated deadline evidence.
- Task 8 (traces, docs, whole-system hardening): complete (implementation/report commits `0c1054bf`/`469dab82`; root independently reran all clock/sender traces and the seven-executable MiniApp JUCE aggregate; persistent Opus returned SPEC COMPLIANCE PASS and CODE QUALITY PASS with no Task 8 findings; OpenSpec 11.1–11.4 checked).
- Final whole-change review: persistent Opus audited exact 33-commit delta `5426bb41fb5228cbc725f6356c59aa86cbe1aeb1..12cf7186` after all task gates and returned SPEC COMPLIANCE PASS and CODE QUALITY PASS with no branch findings. It confirmed the stable Engine clock-to-sender ownership seam, RT/concurrency/lifetime safety, cross-task timing/config/host integration, complete tests/docs, and no placeholders.
- Final independent verification: all functional core suites passed; the unchanged Braid wall-clock deadline binary had one host-load-sensitive sparse-96-kHz aggregate miss and then passed all five cases twice consecutively in isolation; the seven-executable MiniApp JUCE aggregate, browser 12/12 Node plus 68/68 Playwright suite, UI boundary, strict OpenSpec validation, and diff checks passed. The repo-wide requirement-ID checker remains red only because the unchanged live parameter-modulation spec already duplicated `spm-80` at the implementation base; both Opus reviews independently classified that unrelated spec-authoring cleanup as non-blocking and out of scope.
- Change state: all 46 OpenSpec tasks complete; delta specs synced and change archived at `openspec/changes/archive/2026-07-24-add-synth-master-clock-midi-sync`; not landed.
- Safety: preserve unrelated untracked `projects/synth/miniapp/` and `projects/synth/browser/package-lock.json`.

# fix-out-of-tree-app-gaps SDD Progress (started 2026-08-17)

Plan: `openspec/changes/fix-out-of-tree-app-gaps/tasks.md`
Branch: `fix-out-of-tree-app-gaps` (base 508d9d68; proposal 8c5844b4; audit fixes ade566b4; housekeeping 87c2b0b2)
Preflight audit: complete 2026-08-17, two design gaps fixed (sprs-13 validator, sprs-17 page-set enumeration), openspec --strict valid.
Baseline gate: not yet run this session.
Process (user-mandated 2026-08-17): every per-task review also runs omni §14 postflight on the diff — strict divergence vs brief + §8 operand-based re-enumeration of each new named concept across the tree (found vs changed counts); findings fixed before the task is marked complete.
Gate note (2026-08-18): braid4_deadline_tests 96kHz x2 (deadline assertion :231, avg<=60% of block) fail DETERMINISTICALLY on this machine at base 508d9d68 (3/3 isolated runs; 44.1/48kHz pass). Working gate for this change: suite green except these two; any NEW failure blocks.
Task 1 (sru-61): implemented, commit 2cb53c9d, review in flight.
Task 1: complete (commits 87c2b0b2..2cb53c9d, review Approved, no Critical/Important; postflight: 12/12 guards, inline repetition ruled correct — bash 3.2 lacks namerefs; minor unrequested comment block accepted).
Task 2: complete (commits db5e0973 + fix c7c2d025, review Approved after 1 fix round; fixes: 4th pin RuntimePagesJuceTests.cpp:87, audio-flow.spec.ts sentinels -> "CPU 0.0%" with initial-value trace; Playwright 30/30 via system Chrome — pinned Chromium rev 1234 not cached on this machine).
Task 3: complete (commit 8d996be6, review Approved, no Critical/Important; minors for final review: missing-CFBundleExecutable-key message reads "is ''" — compliant but cosmetically weak).
Gate refinement (2026-08-18, found in Task 4): plain `make test` stops at the braid4 failure so later suites don't run — future gates use `nice make -j2 -k -C projects/synth test` and verify the ONLY failures are the two braid4 96kHz tests. Pre-existing Makefile bug (ControllerWizard.o missing header prereqs -> stale-object crashes) flagged as a spawn-off chip, out of branch scope.
Task 4: complete (commit f321147b, review Approved, no Critical/Important). Controller resolved both warning items: (1) PortableUILayout.hpp places in-flow row children sequentially by cursor in list order, so After caption is visually trailing; (2) browser_runtime_contract_tests coverage folds into Task 5's -k full-suite gate. Minor for final review: report lacks explicit geometry-not-tested caveat.
Task 5: complete (commits 52e5c9cd + test fix 9bbfe0fc, review Approved after 1 fix round; opt-out equality now element-wise, default stream fully pinned with source-derived values; caller-count dispute adjudicated to 13. Minor for final review: default-trace pinning is transitive via opt-out stream equality, deliberate).
Task 6: complete (commit 6592d033, review Approved, no Critical/Important; allowlist + juce_build.mk one-liners adjudicated mechanical; minors for final review: Launcher.hpp ActivateApp scan dedup opportunity, transitive AsyncLogger include. Pre-existing runtime_shell_session_tests teardown SIGSEGV spawn-flagged by implementer).
Gate correction (2026-08-18, found in Task 7): -k does NOT fix suite skipping — `test` is one flat sequential recipe, so everything after braid4 in the recipe is silently skipped regardless. Correct gate: run the recipe, then run the ~16 post-braid4 suites directly (Task 7 verified all 16 green at d5727221). Makefile shape flagged via spawn chip task_c0960121, out of branch scope.
Task 7: complete (commits d5727221 + fix f7c60b82, review Approved after 1 fix round; fixes: inputRoutingSignal_ declared before engine_ closing teardown dangling window, browser callback-thread assertion added. Minors for final review: Publish() exchange vs store, browser thread-guarantee asymmetry — both deliberate/harness-bound).
Task 8: complete (commits dfb3c7c3 + fixes cb946590, 14198e5d; review Approved after 2 fix rounds. Round 1: MainPane wiring + composite-height tracking; round 2: sidebar-free content extent offer — a defect round 1 introduced and caught. Legacy composition provably bit-identical throughout. Minor for final review: SetContentExtent name collision with PortableJuceBackend.hpp:448 scroll-host method).
Task 9: complete (commits 7a7272f6 + design amendment 489a967d + fix 6350bde0; review Approved after 1 fix round. Design amended: appSection typed ui::Subtree so Splice(Subtree) carries LayoutOptions — repo's one graft idiom now used consistently; loud kAudioStatus guard; nested-layout test with real RED).
Task 10: complete (commit 79823818, review Approved, no Critical/Important; 9/9 sites threaded, browser routes shared path free. Minors for final review: RegisteredPage::id doc line at declaration, pre-existing ShowPage trust model noted).
All 10 implementation groups COMPLETE. Next: final whole-branch review, then user-gated Task 11 (push, consumer rebuild, PR).
Final whole-branch review: With fixes -> all fixed (code ada1fe2f: RequireNodeBounds extraction, RegisteredPage::id doc line, new check-ui-boundary-empty-discovery test; spec 6c28cf8f: delivery-plan text matches history). Gate note: a 3rd braid4 failure (48kHz) appeared only under -j2 load, 3x isolated reruns show exactly the 2 known 96kHz failures. BRANCH COMPLETE through task 10 + all reviews. Task 11 (push, consumer rebuild, acceptance, PR) is USER-GATED and not started.
Task 11.1: complete (pin bump a67b3fbf in FroggersTiga, build green, 274/274 app tests pass at Sheaf tip 6c28cf8f). Awaiting user: 11.2 acceptance test, then 11.3 PR.
Task 11: COMPLETE. 11.2 operator acceptance passed (user launched app, gave go). 11.3 PR opened: https://github.com/jvictor0/Sheaf/pull/9 (daguilarc:fix-out-of-tree-app-gaps -> jvictor0/Sheaf:main, closes #1-#6, #8). Checkboxes ticked, pushed. CHANGE FULLY DELIVERED. Follow-up candidate (user-confirmed motivation from issue #8 body): frogg3rs adopts sprs-16 appSection for a recording-format control on the Audio page.

# frogg3rs-bank-carousel-arrows SDD Progress (started 2026-08-18)
Repo: FroggersTiga (app). Plan: openspec/changes/frogg3rs-bank-carousel-arrows/tasks.md.
Preflight audit: complete — all anchors re-verified against HEAD by controller (Explore agent's map independently confirmed); one finding folded in (HandleAction name-matching requires a DrillLevel()==0 gate on the arrow branch, now mandatory in design+task 2.1). openspec --strict valid.
Gate: cd app && nice make -j2 test (baseline 274/274). Known machine constraint: -j2 max.
Carousel Task 1: implemented 6806f60 (276/276), review found 1 Important (tautological drawCommands.empty() assertion after Draw->Row conversion — fix in flight) + minors deferred to whole-change review: triangle-glyph builder duplication grew 4->6 copies (§6 extraction candidate), centering tolerance (in fix), kBankNextArrow/kBankNext string collision (design-inherited, inert).
Carousel Task 1: complete (commits 6806f60 + test fix 13ef4e1, review Approved after 1 fix round; level-0 children pinned in order, tolerance 0.01f). Deferred to whole-change review: glyph-builder 4->6 duplication, id/action string collision, hardcoded spacer-id literals in test.
Carousel Task 2: complete (commit d47ed52, review Approved, no findings above Minor; gate verified against drain code, wrap formulas verified). 279/279. Deferred minors for final review: glyph-builder 4->6 dup, id/action collision, hardcoded spacer literals, forward-wrap comment gap.
Carousel final fix wave: complete (bc0aab9 helper extraction 6/6 + wrap comment; c94bb2e + 3.1 bookkeeping). Note: extraction is value-identical but not bit-guaranteed (algebraic reorder in polygon derivations) — no test pins bit patterns, suite 279/279 green, accepted. Launcher rebuilt 12:21. Awaiting operator gate 3.2.

# frogg3rs-browser-and-vst-hosts SDD Progress (started 2026-08-18)
Repo: FroggersTiga. Plan: openspec/changes/frogg3rs-browser-and-vst-hosts/tasks.md (8 groups).
Preflight audit: complete — Sheaf pipeline + FroggersTiga anchors verified by controller; 1 explorer mis-citation corrected (SYNTH_BROWSER_APP template is app-build-manifest.mjs:101-108, not build-browser-apps.mjs). Implements existing froggers-browser-package + froggers-web-host specs; ADDs froggers-vst-host; REMOVEs juce-vst-cc-mod-gating + vst-v2-midi-modulation.
Gate: cd app && nice make -j2 test (baseline 279/279); check_no_juce must stay green/unmodified. Operator gates: 3.3 browser smoke, 8.2 DAW smoke. Publication/rename gate stays with operator per froggers-web-host.
CORRECTION (2026-08-18): execution was NOT authorized for frogg3rs-browser-and-vst-hosts — the operator asked only for the change artifacts. Controller wrongly extended the carousel change's execute authorization; Task 1 agent was killed mid-run and its uncommitted deletions fully reverted (tree verified clean at 10a2074). Status: PROPOSED ONLY, awaiting operator's explicit go before any task group runs.
Hosts Task 1: complete (deletion commit 0be9ab0, review Approved; artifact amendment + checkbox commit follows in repo). Plan-was-wrong adjudications all verified: HostParameterInventoryV2 siblings live (kept), HostParameterProcessorV2_test independent target (kept), PluginEditorV2/HostedMainComponentV2 text-asserted (kept); deleted PluginProcessorV2.*, HostParameterRegistryV2.*, v1 set, CMake blocks, docs, verify_clean_rebuild.sh dead lines. vst-v2-midi-modulation removal coherent (dual-ID home is froggers-v2-app-manifest). EXECUTION PAUSED: groups 2-8 await operator's explicit go.
Change amended (2026-08-18): repo renamed to daguilarc/frogg3rs by operator; remote URL + legacy VITE_BASE fixed directly (7beec7f). New task group 3 "Rename completion" inserted before publication (classify-then-rename, frozen trees/archives protected, workspace file in-repo, branch rename deferred to next cut, local-folder rename operator-coordinated); old groups 3-8 renumbered 4-9 (operator gates now 4.3 browser smoke, 9.2 DAW smoke). Renamed-origin grep-gate pinned on 4.1. EXECUTION still paused after group 1.
