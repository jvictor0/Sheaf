# Main Quest

- Main Quest: `obsidian_chat`
- Path: `../../main_quests/obsidian_chat/`
- Reason Split Out: Decide whether to restore the legacy REST metadata/history endpoints or retire the stale iOS helpers without blocking the websocket-first Obsidian chat pane.
- Source Issue: `The iOS client model still references legacy thread metadata and messages REST endpoints that the live server no longer exposes.`
